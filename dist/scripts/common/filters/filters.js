/**
 * Created by shihao on 16/5/31.
 */

/*
'use strict';
presto.FILTERS
    .filter('searchFilter',['CommonMethods',function(CommonMethods){
        return function(item,searchText){
            return !CommonMethods.isStringEmpty(searchText) && item.productName.indexOf(searchText) != -1;
        };
    }]);*/
